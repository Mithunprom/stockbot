# StockBot Program Review — 2026-W29 (Jul 13–15)
*Program Office: TPM + PM | First weekly review*

## Scoreboard vs Targets

| Metric | Target | Actual | Status |
|--------|--------|--------|--------|
| M1 Uptime streak | 14 days w/o critical watchdog event | 5 days (since Jul 10) — **unverifiable** (endpoints 403) | 🟡 |
| M2 Measured edge | PF ≥ 1.2, n ≥ 100 @ v0.4.4 | n ≈ 15 est. (Jul 14); PF unknown | 🟡 85 trades short |
| M3 H1 validation | Backtest: both legs improve | 0 data runs complete | 🔴 BLOCKED |
| M4 H5/H3 validation | Same walk-forward standard | 0 data runs complete | 🔴 BLOCKED |
| M5 Paper gate | Sharpe ≥ 1.5, DD ≤ 8%, 3 months | Not started | ⚪ |
| Hypothesis data runs | ≥ 1 of 4 merged hypotheses validated | 0 / 4 | 🔴 ALL BLOCKED |

**⚠️ Live data unavailable.** Railway endpoints return HTTP 403 for **4 consecutive days** (Jul 12–15). M2 trade count, watchdog status, and PF cannot be confirmed. All figures are estimates from last readable state (Jul 14 snapshot).

## Decisions Needed from Owner

1. **[CRITICAL — unblocks everything] Enable Railway worker service.** Set `AGENT_WORKER_ENABLE=true` + Alpaca paper API keys in Railway dashboard. Unblocks M3, M4, and all four pending hypothesis backtests. Est. 10 min.
2. **[MONITORING] Investigate endpoint 403.** Are `/portfolio/summary`, `/trades`, `/watchdog` reachable from outside the Claude sandbox? If yes, the gap is sandbox-specific — consider external uptime monitoring (e.g. UptimeRobot) so M1 and M2 can be tracked autonomously.
3. **[SCOPE] Confirm compound M2 baseline.** v0.4.4 freeze includes H4 + H2 + H5 + H3 deployed simultaneously. M2's PF result will reflect all four; individual attribution is impossible without isolating backtests. Confirm this compound baseline is acceptable, or decide which hypotheses to hold back.

## What Shipped This Week

- **H4** — Ensemble renorm (dead-weight drop): LGBM 0.60→0.75, Sentiment 0.20→0.25. PR #3 merged Jul 10. No data run yet.
- **H2** — SPY 20d-MA regime overlay: halves new-entry notionals when SPY below MA. PR #4 merged Jul 11. No data run yet.
- **H5 + H3** — Signal-reconfirmed hold extension (max 3d) + earnings blackout gate. PR #6 merged Jul 14. No data run yet.
- **v0.4.4 bugfix** (Jul 13) — Live equity chart stale data + broker value mismatch corrected. Bug-fix; freeze-exempt.
- **H1 draft PR #7** (Jul 15) — Cross-sectional top-k ranker: 30/30 tests green, correctly held as DRAFT pending Railway backtest. **Freeze intact.**
- **Program Office onboarded** (this run) — TPM + PM personas, ROADMAP.md, weekly review cadence established.

## Milestone Deltas

- **M1** clock: 5 of 14 days clean — but watchdog unreadable. Streak may have broken; cannot confirm.
- **M2** clock: n ≈ 15 / 100. At ~2 trades/day pace → n=100 around early August. PF measurement starts then.
- **M3 / M4**: Day 5+ blocked on Railway worker. H4 now hits the 5-day flag threshold.

## THE Bottleneck (TPM)

**Railway worker service** (`python agent_worker.py`, env `AGENT_WORKER_ENABLE=true` + Alpaca keys).

Every pending hypothesis validation is blocked. H4, H2, H5, H3 are merged, deployed, and accumulating live trades — but have zero backtest evidence. If M2 misses PF ≥ 1.2, we cannot diagnose which hypothesis hurt. The worker unblock is a 10-minute owner action and is the single highest-leverage item on the board.

Secondary: Railway endpoint 403 from the Claude sandbox proxy is an independent monitoring gap (4 days).

## Top Risks (Updated)

| Risk | Severity | Status |
|------|----------|--------|
| Railway endpoint 403 — monitoring blind | HIGH | **NEW** — 4 days running; M1 unverifiable, M2 unreadable |
| Attribution blur — 4 hypotheses merged w/o individual data runs | HIGH | **NEW** — compound baseline at v0.4.4 |
| Railway worker not running — all validation blocked | HIGH | Ongoing; owner action pending (>5 days) |
| H4 stuck in needs_data_run >5 days | MED | **FLAGGED** — Jul 10 → Jul 15 = 5 days at threshold |
| Long-only assumption landmines | MED | Audit queued for M2 window |
| Mid-sample config churn (4× last week, then frozen) | MED | Mitigated — hard freeze at v0.4.4 |

## PM Assessment — Are We Moving the North Star?

**Honest answer: yes in code depth, no in measurement.** The desk shipped four non-trivial hypotheses in five days and kept the test suite green. That is genuine progress on signal quality. However, none of the hypotheses has a data run; every "expected impact" claim is theoretical. The program is building a validation debt that can only be cleared when the Railway worker runs. Until then, M2 is a clock ticking toward an unknown outcome.

The freeze is the right call. What is needed next is not more implementation — it is the Railway worker.
